Paste the following in the terminal to create a virtual environment
python -m venv venv

Activate the environment with
venv/scripts/activate

Paste the following in the terminal to install the required packages
pip install -r .\rule\requirements.txt
python -m spacy download en_core_web_sm
